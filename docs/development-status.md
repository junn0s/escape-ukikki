# 개발 현황

> 갱신일: 2026-07-29
> 기준 문서: `production-roadmap.md`, `game-design-document.md`, `technical-design-document.md`

이 문서는 기획 변경을 위한 문서가 아니라, 설계 문서가 Unity 프로젝트에 어디까지 반영됐는지 기록하는 작업 현황표다.

## 현재 단계

- M0 프로젝트 기반: 완료
- M1 로컬 버티컬 슬라이스: 진행 중
- M2 네트워크 기반 이후: 시작 전

## 이번 구현 완료

### 플레이어와 카메라

- `WASD` 이동
- 마우스 위치를 향한 캐릭터 회전
- 회전하지 않는 고정 쿼터뷰 추적 카메라
- `CharacterController` 기반 벽 충돌
- `F` 손전등 켜기·끄기

### 맵과 상호작용

- 연구소 10개 방 회색 박스 유지
- 각 방 외곽 벽과 통로 출입구 충돌 생성
- 6개 시작점 중 첫 번째 위치에 로컬 플레이어 배치
- 거리 기반 상호작용 대상 탐색
- 화면 하단 `E` 상호작용 안내
- 전력 복구실 퓨즈 스테이션 배치
- 번호와 색상을 함께 표시하는 3개 퓨즈 순서 미니게임
- 잘못된 퓨즈 선택 시 즉시 실패하고 다시 상호작용해 재시도
- 성공 시 스테이션 표시등과 본체 색상 복구
- 미션 중 이동·조준 잠금, `Esc` 취소 후 입력 복구
- 퓨즈 오답 1회마다 `Medium` 소음 사건 1회 발행
- 퓨즈 실패 소음은 전력 복구실 기준 NavMesh 경로 거리 14m까지 전달

### 소음과 괴물 AI

- 연구소 회색 박스 맵용 NavMesh 베이크 및 재빌드 시 에셋 GUID 유지
- 전력 복구실·격리실 동쪽 구간을 순찰하는 프로토타입 원숭이 1마리 배치
- 순찰 속도 2.6m/s, 방 체류 6±1초
- 소음 조사 시작 속도 6.0m/s, 최대 가속 6초 후 순찰 속도로 감속
- 소음 지점 도착 후 3초 탐색하고 다시 순찰
- 경로 거리, 소음 크기, 최신 시각, 작은 `noiseId` 순서의 결정적 우선순위 적용
- 소음 발생 배너와 상태별 프로토타입 색상 표시

### 검증

- Unity 에디터 컴파일 및 씬 구성 검증 통과
- EditMode 자동 테스트 15개 통과
- 테스트 대상: Unity 버전, 렌더 파이프라인, 릴리스 씬, 입력 액션, 첫 플레이어블 씬 구성, 퓨즈 성공·실패·취소·중복 입력 방지, 소음 우선순위와 경로 반경 판정
- 실제 재생에서 퓨즈 실패, 재시도, 성공과 플레이어 입력 잠금·복구 확인
- 실제 재생에서 퓨즈 실패 시 경로 거리 9.4m의 원숭이가 소음 조사 상태로 전환되는 것 확인

## 직접 실행 방법

1. Unity에서 `Assets/_Project/Scenes/10_Laboratory.unity`를 연다.
2. 상단 재생 버튼을 누른다.
3. `WASD`로 이동하고 마우스로 바라본다.
4. 전력 복구실의 주황색 퓨즈 스테이션 가까이에서 `E`를 누른다.
5. 오른쪽 슬롯에 표시된 순서대로 왼쪽 퓨즈를 클릭한다.
6. 일부러 틀린 퓨즈를 눌러 소음 배너와 원숭이의 노란색 조사 상태를 확인한다.
7. 원숭이는 소음 지점에서 보라색 탐색 상태를 거친 뒤 다시 순찰한다.
8. `Esc`로 미션을 취소하거나 `F`로 손전등을 켜고 끈다.

## M1 다음 구현 순서

1. 플레이어 시야·후각 감지와 추격
2. 괴물 물기와 피격 판정
3. 로컬 감염 타이머와 해독제
4. 한 명이 미션 실패부터 감염까지 경험하는 M1 전체 플레이 테스트

M1 완료 조건은 한 명이 이동해 미션을 실패하고, 소리를 조사하러 온 괴물을 피해 도망치며, 물리면 감염되는 흐름이 로컬에서 끊김 없이 작동하는 것이다.
