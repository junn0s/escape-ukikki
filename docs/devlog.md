# 개발 기록 (devlog.md)

> 목적: 실제 개발 세션에서 있었던 결정·변경 사항을 짧게 남긴다.
> `production-roadmap.md`(계획서 — 앞으로 무엇을 할 것인가)와 달리, 이 문서는
> "실제로 무엇을 했는가"만 사후에 기록한다.
>
> 작성 규칙은 `.claude/skills/game-doc-sync/SKILL.md`와 `AGENTS.md`의 "개발 기록" 절을 따른다.
> 항목당 3줄 내외로 간결하게 유지한다.

---

<!-- 아래부터 실제 기록을 추가하세요 -->

## 2026-07-29
- 구현: Unity 프로젝트 기반과 첫 플레이어블, 6개 시작점 재배치 및 바닥 추락 회귀 검사
- 기획서와 달라진 점: 없음
- 다음 할 일: 퓨즈 실패 소음, 원숭이 NavMesh 순찰·조사, 물기와 로컬 감염 구현

## 2026-07-29
- 구현: 전력 복구실 퓨즈 순서 미니게임, 즉시 실패·재시도·성공과 입력 잠금
- 기획서와 달라진 점: 없음
- 다음 할 일: 퓨즈 실패를 Medium 소음 사건으로 연결하고 괴물 한 마리의 조사를 구현

## 2026-07-29
- 구현: 퓨즈 실패 Medium 경로 소음과 NavMesh 원숭이 순찰·조사·탐색, 상태 시각화
- 기획서와 달라진 점: 없음
- 다음 할 일: 플레이어 시야·후각 추격, 물기와 로컬 감염을 구현

## 2026-07-29
- 구현: 30초 시작 보호, 원숭이 시야·후각 추격, 물기 판정·재물림 보호와 강화 단계 SO
- 기획서와 달라진 점: 없음
- 다음 할 일: 물기를 로컬 감염 타이머와 해독제 사용 흐름에 연결

## 2026-07-29
- 구현: 물기 연동 감염 타이머, 독성 단계 고정·재감염 유지, 해독제 사용·이동 취소와 HUD
- 기획서와 달라진 점: 없음, 해독제 제작·획득은 계획대로 M5에서 구현
- 다음 할 일: Unity Services 인증과 MPS Session·Relay 기반 M2 네트워크 구현

## 2026-07-29
- 구현: 물기 성공 후 3초 추격 해제, 소리 조사 중 근접 후각 감지 전환, 스폰 표시 구체 제거
- 기획서와 달라진 점: 사용자 피드백에 맞춰 SDD·밸런스 문서의 괴물 공정성 규칙을 보완
- 다음 할 일: Unity Services 익명 인증과 MPS Session·Relay 기반 M2 네트워크 구현

## 2026-07-29
- 구현: Bootstrap 순차 초기화, Unity Services·익명 인증 상태와 실패 안내·재시도 기반
- 기획서와 달라진 점: 없음, Unity Cloud 미연결은 사용자 안내 상태로 처리
- 다음 할 일: Cloud 연결 후 실제 인증 검증과 MPS Session·Relay 방 생성·참가 구현

## 2026-07-29
- 구현: 실제 익명 인증, 6명 비공개 MPS Session·Relay 방 생성·코드 참가·종료와 메인 메뉴 화면
- 기획서와 달라진 점: 없음
- 다음 할 일: Multiplayer Play Mode Host+Client 연결과 로비 참가자·준비·색상 동기화

## 2026-07-29
- 구현: 서버 권위 로비 슬롯·6색·준비 상태와 HostClient2 실제 Relay 2인 자동 검증
- 기획서와 달라진 점: 없음
- 다음 할 일: 네트워크 씬 전환과 참가자별 Player NetworkObject 생성

## 2026-07-29
- 구현: 감염 중 괴물 동료 판정, 네트워크 씬 전환과 참가자별 Player NetworkObject 기반
- 기획서와 달라진 점: 사용자 지시에 따라 감염 중 재물림 연출을 제거하고 해독 후 감지를 복구
- 다음 할 일: Host+Client 수동 검증 후 괴물·물기·감염 서버 권위 이전

## 2026-07-29
- 구현: 2D 탑다운 카메라·물리·맵·원숭이 경로·절차형 스프라이트와 네트워크 플레이어 전환
- 기획서와 달라진 점: 승인된 시점 변경을 GDD 1.1과 하위 제작 문서에 동기화
- 다음 할 일: 사용자 로컬·Host+Client 수동 확인 후 괴물·감염 서버 권위 이전

## 2026-07-30
- 구현: 외곽선 기반 직각 복도, 본체 고정·손전등 조준 피벗, 괴물 직선 추격·표면 물기 판정 반영
- 기획서와 달라진 점: 피드백에 맞춰 맵 설계 1.5와 후각 2.5/4/6m·소음 12/26/40m를 동기화
- 다음 할 일: 로컬 추격·물기·감염·순찰 및 퓨즈 소음 검증 완료, 괴물·감염 서버 권위 이전

## 2026-07-30
- 구현: 온라인 공용 게임 시스템 유지·소유자 상호작용, 방 내부 6인 스폰과 다중 대상 원숭이 감지
- 기획서와 달라진 점: 승인된 무시야 원형 근접 감지·8m 소음 현장 급습을 GDD 1.2와 밸런스에 반영
- 다음 할 일: HostClient2 수동 확인 후 괴물·미션·감염 판정을 서버 권위로 이전

## 2026-07-30
- 구현: 초기 괴물 4마리 생성과 Medium 소음 반경 내 전체 괴물 동시 반응
- 기획서와 달라진 점: 없음, 퓨즈 실패 소음 범위를 전력 복구실 인접 구역 기준으로 명시
- 다음 할 일: 퓨즈 실패 전후 4마리 반응 범위 수동 확인 후 서버 권위 이전

## 2026-07-30
- 구현: 네트워크 플레이어 조준 카메라 재결합, 물기·감염 UI 연결과 괴물 간 물기 경로 방해 제거
- 기획서와 달라진 점: 없음
- 다음 할 일: HostClient2 손전등 조준·물기 확인 후 괴물·감염 서버 권위 이전

## 2026-07-30
- 구현: 평상시 원형 근접 감지 반경을 2.3/3.1/4.1m로 소폭 확대
- 기획서와 달라진 점: 플레이 테스트 피드백에 따른 밸런스 수치 조정
- 다음 할 일: 기본 단계 체감 확인 후 괴물·감염 서버 권위 이전

## 2026-07-30
- 구현: 서버 상호작용 거리·경로·순서 검증, 퓨즈 독점 점유와 소유자 전용 역할 공개
- 기획서와 달라진 점: 없음
- 다음 할 일: HostClient2에서 개인 역할·동시 점유·연결 종료 해제 확인 후 M3 시작

## 2026-07-30
- 구현: 서버 라운드·15분 시계·개인 20% 미션·프로젝트 단계·승패와 괴물·감염 동기화
- 기획서와 달라진 점: M3 회색상자는 퓨즈 변형 5개를 재사용하며 나머지 미니게임은 후속 제작
- 다음 할 일: HostClient2에서 100% 승리·시간 초과·괴물·감염 결과 일치 확인

## 2026-07-30
- 구현: 퓨즈·차단기·CCTV·시료 분류 4종 회색상자와 시작 거리 기반 개인 미션 순서 배정
- 기획서와 달라진 점: M3에서는 공통 순서 입력을 재사용하며 유형별 전용 조작은 M5에서 완성
- 다음 할 일: HostClient2에서 개인 목록·100% 승리·시간 초과와 괴물·감염 결과 일치 확인

## 2026-07-30
- 구현: 온라인 카메라 재스냅 제거, 네트워크 상호작용 안내 연결, 미션 소음 경로 범위 보정
- 기획서와 달라진 점: 플레이 테스트 결과 Medium 소음 범위를 26m에서 30m로 조정
- 다음 할 일: HostClient2에서 이동·상호작용 안내·미션 실패 괴물 반응 재확인

## 2026-07-30
- 구현: 원숭이 4개 독립 순찰 경로와 전원 공개 미션 수행·완료 순간 연출 추가
- 기획서와 달라진 점: 없음, 개인 미션 완료 정보는 소유자 전용으로 유지
- 다음 할 일: HostClient2에서 평상시 순찰 분산과 빌런 화면의 미션 공개 연출 확인

## 2026-07-30
- 구현: M4 빌런 강화 3축(후각·개체·독성) 서버 권위 판정, 축별 강화 스테이션 3개, 개체 강화 3초 예고 후 격리실 A/B 2마리씩 추가
- 기획서와 달라진 점: 없음, 강화 단계는 브로드캐스트하지 않고 빌런 소유 클라이언트에만 전달
- 다음 할 일: HostClient2에서 강화 판정과 생존자 화면 비공개 확인 후 스피커·단서 착수

## 2026-07-31
- 구현: 현장 단서 3종(붉은 연기·격리실 잠금·빈 주사기)을 강화 성공에 연결하고 라운드 종료까지 영구 유지
- 기획서와 달라진 점: 없음, 강화 스테이션 위치를 GDD 13.2~13.4에 맞춰 보안실·백신실 B로 교정
- 다음 할 일: HostClient2에서 단서 공개·영구 유지 확인 후 스피커와 붉은 LED 단서 착수

## 2026-07-31 (2)
- 구현: 스피커 리모컨(빌런 전용 지도, 45초 쿨타임, Large 40m 소음)과 붉은 LED 흔적으로 단서 4종 완성
- 기획서와 달라진 점: 없음, 쿨타임 회의 중 정지는 회의 단계 구현 시 연결 예정
- 다음 할 일: HostClient2에서 스피커 검증 후 빌런 가짜 미션 또는 회의·투표 착수

## 2026-08-01
- 구현: 빌런 가짜 미션. 생존자와 같은 목록·연출을 주되 완료 시 프로젝트 진행률은 오르지 않게 분리
- 기획서와 달라진 점: 없음, 서버가 역할로 완료 경로를 나누고 기존 생존자 검사도 방어선으로 유지
- 다음 할 일: HostClient2에서 가짜 미션 진행률 미반영 확인 후 회의·투표 착수

## 2026-08-02
- 구현: 회의·투표·퇴출. 회의 3단계를 라운드 상태머신에 추가하고 탐색 타이머를 회의 중 정지
- 기획서와 달라진 점: 없음, 빌런 퇴출이 기존 승패 판정의 isVillainExiled 입력에 연결됨
- 다음 할 일: HostClient2에서 회의 흐름 검증 후 감염·제작 타이머 정지와 유령 이동 착수

## 2026-08-02 (2)
- 구현: 유령 이동·비가시성, 회의 중 감염 타이머 정지, 보안실 스피커 로그(50% 이후 열람)로 M4 작업 9개 완료
- 기획서와 달라진 점: CCTV 카메라 전환 화면은 서버 로그 열람으로 대체, 유령 채팅은 일반 채팅과 함께 MVP 제외
- 다음 할 일: HostClient2 전체 검증 후 M5(해독제 경제, 미션 UI 4종, 맵 구조) 착수

## 2026-08-03
- 구현: M5 해독제 경제. 개인 레시피 후보 8곳 배치·발견, 백신실 제작기 2대 180초 제작,
  완성품 선착순 획득, 보관 칸, 회의 중 제작 정지와 감염 사망 시 보관 칸 이관
- 기획서와 달라진 점: 없음. 문서에 없던 보관 칸 슬롯 수(칸당 2개)를 밸런스 §8에 추가하고
  SO 필드 매핑 표를 함께 넣음
- 다음 할 일: Unity에서 컴파일·씬 검증·EditMode 테스트 실행 후 미션 UI 4종 착수

## 2026-08-03 (2)
- 구현: 토론 텍스트 채팅과 스피커 쿨타임 회의 중 정지. 채팅은 살아 있는 참가자에게만
  대상 지정 Rpc로 보내고, 각괄호를 지워 Rich Text 태그 삽입을 막음
- 기획서와 달라진 점: 없음. 문서에 없던 채팅 수치(80자·1초 간격·60개 보관)를 밸런스 §2에 추가.
  유령이 채팅을 볼 수 없다는 규칙 때문에 NetworkList 복제를 쓰지 않음
- 다음 할 일: Unity 검증 후 미션 UI 4종 또는 로비 복귀·재시작 착수

## 2026-08-03 (3)
- 구현: 로비 복귀·다시하기와 라운드 상태 초기화, 결과 화면 상세(역할·강화·단서),
  연결 종료 30초 유예와 빌런 이탈 시 생존자 승리, 회의 후 2초 물기 보호,
  이름표와 Tab 미션 목록·전자지도
- 기획서와 달라진 점: 없음. 재접속 신원 복원과 미복귀 생존자의 공용 미션 이관(GDD 19.2)은
  선행 작업이 커서 미구현으로 남김. 결과 타임라인은 컷라인에 따라 제외
- 다음 할 일: Unity 컴파일·테스트 후 미션 UI 4종 설계 확정(미니게임 규칙이 문서에 없음)

## 2026-08-03 (4)
- 구현: 배치 모드 검증. EditMode 226개 전부 통과, 씬 빌드·검증 통과.
  `PlayerNameTagView`에서 `MonkeyLab.Presentation.Camera` 네임스페이스와 `UnityEngine.Camera`가
  충돌한 컴파일 에러 1건 수정
- 기획서와 달라진 점: 없음
- 다음 할 일: HostClient2 수동 검증, 미션 UI 4종 규칙 확정

## 2026-08-03 (5)
- 구현: 괴물 순찰 목적지 분산, 로컬 해독제 레시피·제작·보관, 거부 사유 UI, HostClient2 단일 `run/deploy` 노드와 표준 Play Mode 전환 추가
- 검증: 씬 재생성·검증, EditMode 230개, 로컬 괴물·감염·퓨즈, Host+Client Relay 2인 연결 통과. 기획서와 달라진 점 없음
- 다음 할 일: 6인 반복 플레이테스트와 미션 UI 4종 상세 규칙 확정

## 2026-08-03 (6)
- 구현: 빌런 물림 연출·감염 면역 네트워크 동기화, 레시피 기록 결과 안내, 10개 방 미션 스테이션과 5개 분산 배정, HostClient2 즉시 활성화 및 결과 HUD 수명주기 오류 수정
- 검증: 씬 재생성·검증, EditMode 234개, 로컬 괴물·감염·퓨즈, Host+Client Relay 2인 연결 통과. 기획서와 달라진 점 없음
- 다음 할 일: 6인 연속 3판 플레이테스트와 미션 UI 4종의 서로 다른 세부 규칙 확정

## 2026-08-03 (7)
- 구현: Unity PlayerId 연결 승인과 Relay 자동 재접속, 30초 안에 역할·위치·미션·감염·레시피·해독제·진행 중 투표를 복원
- 검증: Unity 컴파일, EditMode 247개, Host+Client Relay 2인 연결 통과. 기획서와 달라진 점 없음
- 다음 할 일: 강제 연결 종료 재접속 실측, 6인 연속 3판과 미복귀 생존자 공용 복구 미션 이관

## 2026-08-03 (8)
- 구현: 퓨즈·시료 드래그, 차단기 레버, 실시간 선이 보이는 CCTV 배선과 동일 시드 서버 검증, 난이도 기반 개인 4~5개·최소 3종 배정
- 기획서와 달라진 점: 없음. CCTV 오배선은 시간만 잃고 나머지 오조작은 실패 소음을 내며, 요청에 따라 테스트 실행은 생략
- 다음 할 일: 월드 위험 경고와 미션 성공·실패 애니메이션, 배터리 운반·압력 밸브 등 확장 미션, 6인 플레이테스트

## 2026-08-03 (9)
- 구현: 서버 추적 대상 기반 미션 화면 가장자리 위험 경고, 성공 확산·실패 흔들림·스테이션 점멸 연출 추가
- 기획서와 달라진 점: 없음. 경고는 방향·거리 없이 자기 자신이 실제 추적될 때만 표시하며, 요청에 따라 테스트 실행은 생략
- 다음 할 일: 비상 배터리 운반과 압력 밸브 미션, 미션별 전용 아트·사운드 적용

## 2026-08-03 (10)
- 구현: 배터리 분리·감속 운반·수신 단말기 장착·낙하 소음과 두 원형 밸브·압력계·안정화·잠금 레버 미션 코드 추가
- 기획서와 달라진 점: 없음. 배터리 수신 단말기와 압력 스테이션의 실제 맵 위치는 레벨 배치 전까지 설정 가능 상태로 유지하며 테스트는 요청에 따라 생략
- 다음 할 일: 레벨에서 배터리 출발/도착 지점과 탈출구 압력 패널 배치, 출입문 보안 해제·서버 로그 복구 미션 구현

## 2026-08-03 (11)
- 구현: 유령 전용 Rpc 채팅, 25/75% 월드 조명·탈출 표시, 헬기·RX-9 엔딩, 7채널 실시간 CCTV와 최종 에셋 카탈로그 연결
- 기획서와 달라진 점: 없음. 6인 Play Mode 시나리오와 Windows x64 빌드 진입점만 코드로 추가했고 요청에 따라 실행·테스트는 생략
- 다음 할 일: `SO_PresentationAssetCatalog_Default`에 최종 프리팩·스프라이트·클립 할당 후 6인 3판과 Windows 빌드 수동 검증

## 2026-08-03 (12)
- 수정: 괴물끼리 물리 충돌로 길을 막지 않게 하고, 이동 진전이 없는 경로를 자동 폐기해 순찰·수색으로 복귀하도록 보강
- 기획서와 달라진 점: 없음. 기존 대기 시간 값을 경로 막힘 판정 시간으로 재사용했으며 별도 밸런스 값은 추가하지 않음
- 다음 할 일: HostClient2에서 좁은 복도 추격과 다중 괴물 물림 후 순찰 복귀 확인

## 2026-08-03 (13)
- 개선: 퓨즈만 드래그로 유지하고 차단기는 타이밍 클릭/Space, CCTV와 시료는 클릭 후 대상 선택 방식으로 분리
- 기획서와 달라진 점: 핵심 규칙은 유지하고 미션별 입력 감각을 구분하도록 UI 조작 명세를 갱신
- 다음 할 일: 배터리 잠금핀 해제와 압력 밸브 회전 미션의 월드 스테이션 배치

## 2026-08-03 (14)
- 밸런스: 괴물 근접 감지를 5/7/9m로 확대하고 표적 상실 후 수색을 5초로 연장해 회피 동선을 요구
- 기획서와 달라진 점: 역할·승패 규칙 변경 없이 괴물 감지와 수색 수치만 문서·SO와 함께 조정
- 다음 할 일: 4인 기본 괴물 상태에서 방 진입 감지 빈도와 평균 첫 물림 시간 확인

## 2026-08-03 (15)
- 개편: 중앙 보안 광장, 북측 시설 띠, 서측 외곽로, 남측 순환로와 동측 뒷골목을 갖는 10개 방·14개 복도 구조로 맵과 순찰 경로 재배치
- 기획서와 달라진 점: 기존 방·미션·승패 규칙은 유지하고 참고 이미지의 다중 순환형 동선만 연구소 테마로 반영
- 다음 할 일: 새 구조에서 문 통과, 괴물 추격 분산, CCTV 프레이밍을 수동 플레이로 확인

## 2026-08-03 (16)
- 고도화: 기존 6종 중 배터리 운반·압력 밸브를 월드에 배치하고 클릭 회전 보안 회로, 방향키 안테나 조율, 키보드 로그 복구를 추가해 9종으로 확장
- 기획서와 달라진 점: GDD에 이미 정의된 보안 해제·안테나·로그 복구를 구체화했으며 완료·실패는 모두 개별 입력을 서버가 재현해 판정
- 다음 할 일: HostClient2에서 9종 입력 감각, 오답 소음, 액체 보관실→입원실 배터리 운반 확인

## 2026-08-03 (17)
- 레벨: 방별 핵심 프롭 68개·공용 설비 40개·복도 표식 30개를 교체 슬롯으로 배치하고, 출입구 간섭 검증을 통과한 고유 자동문 27곳 추가
- 기획서와 달라진 점: 일반 자동문은 잠금 규칙 없이 접근 시 열리며, 서버가 플레이어·괴물 센서와 충돌 상태를 동기화
- 다음 할 일: 최종 프리팹을 `EnvironmentPropSlot` 단위로 교체하고 HostClient2에서 문 끼임·추격 통과 확인

## 2026-08-03 (18)
- 연출: 현재 방·조준점·상호작용 표식·추격 맥동·미션/단계 배너·카메라 충격·보행 바운스·자동문 상태등을 통합
- 기획서와 달라진 점: 없음. 서버 판정 값을 읽는 로컬 표현만 추가하고 괴물 방향·거리 정보는 노출하지 않음
- 다음 할 일: HostClient2에서 추격 경고와 소유자 전용 UI를 확인하고 최종 UI·사운드 에셋으로 교체

## 2026-08-03 (19)
- 조명: 월드를 URP 2D Lit로 전환하고 3.5% 비상광·개인등·방향 손전등과 25/50/75/100% 단계 점등 구현
- 기획서와 달라진 점: 없음. GDD의 0% 약 1m 식별 범위와 단계 보상을 실제 Light2D로 반영
- 다음 할 일: HostClient2에서 0% 시인성, 25% 어두운 구간 유지, 유령 조명 비노출을 수동 확인

## 2026-08-03 (20)
- 이동 연출: 보행 주기와 상하 바운스·스쿼시·기울기를 낮춰 과도한 통통 튀는 움직임 완화
- 기획서와 달라진 점: 없음. 이동·네트워크 판정은 유지하고 로컬 표현 값만 조정
- 다음 할 일: HostClient2에서 로컬·원격 캐릭터의 정지 복귀와 보행 리듬 확인

## 2026-08-03 (21)
- 연결 복구: 미복귀 생존자의 미완료 미션을 공용 목록으로 승계하고 원소유자 점수 예산으로 완료 처리
- 기획서와 달라진 점: 없음. 유령은 개인 미션만 허용하고 살아 있는 생존자만 공용 복구 수행
- 다음 할 일: HostClient2에서 30초 유예 내 복원과 만료 후 공용 목록·100% 도달 확인

## 2026-08-03 (22)
- 개발 도구: F10 서버 패널에 역할·생명·해독제·진행도·시간·강화·승패 조작과 AI/소음 진단 통합
- 기획서와 달라진 점: 없음. 모든 조작은 에디터·Development에서만 허용하고 기존 서버 복제 경로 사용
- 다음 할 일: HostClient2에서 대상별 상태 복제와 4/6/8마리 전환·소음 반경 표시 확인

## 2026-08-03 (23)
- 설정: F1 런타임 설정과 로컬 저장, 해상도·품질·음량 채널·텍스트·흔들림·점멸·비네팅·소리 방향·색상 보조 연결
- 기획서와 달라진 점: 암흑 시야를 보존하도록 밝기 상한을 기본값으로 제한했으며 요청에 따라 플레이 테스트는 생략
- 다음 할 일: 입력 재지정 UI와 최종 AudioMixer 그룹 연결, Windows/HostClient2에서 해상도·설정 유지 수동 확인

## 2026-08-03 (24)
- 레벨: 방별 전용 프롭 112개와 방 구조물 포함 252개 교체 슬롯, 다층 회색상자 표현·복도 반복 설비·교체 기준점 추가
- 동선: 문 안쪽 접근 노드와 중앙 통행 축을 연결하고 문 안전 구역·장애물 중첩 배치 검증 강화. 기획서 핵심 규칙 변경 없음
- 다음 할 일: 최종 프리팹을 ReplacementAnchor에 연결하고 10개 방별 시인성·AI 가구 우회·27개 문 통과 수동 확인

## 2026-08-03 (25)
- 조명: 0% 전역광을 0.2%로 낮추고 개인등 반경을 1.15m로 축소, 원격 플레이어 시야 광원 비노출 처리
- 단계 연출: 25/50/75% 유도등·보안실·탈출로 조명을 국소 반경으로 제한. 기획서 핵심 규칙 변경 없음
- 다음 할 일: HostClient2에서 손전등 밖 암흑과 소유자별 광원 격리, 단계별 국소 점등 수동 확인

## 2026-08-03 (26)
- 감지: 손전등 상태를 서버에 복제해 점등 대상만 평상시 근접 감지, 소음 급습은 소등 예외로 분리
- 시야: 소등 개인등을 점등 밝기의 5%·반경 0.65m로 축소하고 GDD 1.3에 핵심 감지 규칙 반영
- 다음 할 일: HostClient2에서 소등 표적 상실·소음 급습 유지·소유자별 암흑 시야 수동 확인

## 2026-08-03 (27)
- 수정: 소음 방향 UI의 `Camera.main`을 Unity 카메라로 명시해 Presentation 네임스페이스 충돌 제거
- 기획서와 달라진 점: 없음. 외부 코드 편집기 경로 경고는 프로젝트 컴파일과 무관
- 다음 할 일: Unity 재컴파일 뒤 Console의 빨간 오류 소멸 확인

## 2026-08-03 (28)
- 조명 수정: 잘못 연결된 Forward Renderer를 Renderer2D로 자동 교체하고 0% 전역광을 완전히 제거
- 시야: 개인등을 실루엣용 0.6%·0.5m로 축소해 단계 복구 전 월드는 손전등 원뿔에서만 표시
- 다음 할 일: Play 재시작 후 빔 외부 완전 암흑과 25/50/75% 국소 복구 조명 수동 확인

## 2026-08-03 (29)
- 감지 개편: GDD 1.4에 따라 점등 또는 이동 중 발걸음만 평상시 근접 감지에 노출하고 정지·소등 은신 추가
- 네트워크: 호스트가 SO 기준 실제 위치 변화로 발걸음을 판정하며 미션 실패·스피커 소음 급습은 은신 예외 유지
- 다음 할 일: HostClient2에서 소등 보행 감지·정지 표적 상실·소음 급습 예외를 수동 확인

## 2026-08-03 (30)
- 빌런 미션: 자동 채널링을 후각 밸브 드래그·개체 회로 클릭·독성 타이밍 주입의 직접 조작 3종으로 교체
- 서버 판정: 같은 시드의 퍼즐에 개별 입력을 재현하고 성공한 경우에만 강화·단서·공개 수행 연출 적용
- 다음 할 일: HostClient2에서 축별 조작, 오답·취소 초기화, 생존자 화면의 퍼즐 비공개를 수동 확인

## 2026-08-03 (31)
- 배포: 미션·괴물·암흑 시야·맵·멀티플레이 통합 개발분을 GitHub `main`에 푸시
- 조명: 전역광 0%를 유지하고 모든 단계 복구 국소광을 `SO_WorldLightingBalance_Default` 기준 최대 15%로 제한
- 다음 할 일: 25/50/75/100%에서 손전등 우위를 유지하면서 표지와 진행 방향만 식별되는지 수동 확인

## 2026-08-03 (32)
- 역할 미션: 1인은 생존자 연습 모드로 고정하고 멀티 강화 프록시를 일반 미션 패널과 같은 위치·독립 점유로 통합
- 흔적: 강화 2회차 후보 보존과 붉은 연기·파손 잠금장치·빈 주사기 절차형 월드 연출을 서버 복제 상태에 연결
- 다음 할 일: HostClient2에서 같은 패널 동시 수행·상대 퍼즐 비공개·축별 1/2단계 영구 흔적을 수동 확인

## 2026-08-03 (33)
- 익명 흔적: 강화 완료 즉시 공개하지 않고 살아 있는 생존자 7m 관찰 반경이 비면 서버가 월드에 공개
- 서버 판정: 빌런·유령 제외, 대기 마커 예약으로 2단계 흔적 중복 방지, 결과에는 보류 흔적도 포함
- 다음 할 일: HostClient2에서 동시 미션 중 흔적 비노출과 전원 이탈 뒤 익명 공개를 수동 확인

## 2026-08-03 (34)
- 괴물 감지: 평상시는 손전등 또는 서버 발걸음만 5/7/9m에서 감지하고 소등·정지 시 추적·물기 취소 유지
- 강제 급습: 미션 실패·스피커는 범위 안의 모든 괴물이 기존 추격·물기보다 우선해 현장까지 집결
- 다음 할 일: HostClient2에서 소등·정지 회피와 Medium/Large 소음의 전원 집결·8m 은신 무시 확인

## 2026-08-03 (35)
- 원숭이 동선: 전역 순찰 후보·최근 3곳 기억·8m 목적지 예약으로 고정 3방 반복을 동적 분산 순찰로 교체
- 이동 AI: 2.2m 분리 조향, 소음 현장 분산 포위, 2초 막힘 경로 재계산 최대 3회 적용
- 다음 할 일: 4/6/8마리 상태에서 방 커버리지·문 통과·소음 집결 후 포개짐을 수동 확인

## 2026-08-03 (36)
- 조작감: 카메라 추적 보간과 이동 방향 선행 보정을 제거해 플레이어 위치를 즉시 추적
- 구조: 로컬·네트워크 카메라 설정에서 지연 파라미터를 제거. 기획서 핵심 규칙 변경 없음
- 다음 할 일: 보행·대각선 이동·방향 전환에서 즉시 추적 체감 수동 확인

## 2026-08-04 (1)
- 조명: 완전 암흑이 방향 감각을 없애 전역광을 0%에서 4%로 올리고, 빌더가 씬 검증에서 0을
  요구하던 하드코딩을 `SO_WorldLightingBalance_Default` 대조로 교체
- UI 방향: 어두운 월드에서 글자가 안 읽히는 문제로 UI를 IMGUI에서 uGUI+TMP로 재구축하기로
  정하고, 형태 문법은 카툰 소셜 디덕션 관행을 따르되 캐릭터·방·아이콘은 RX-9 고유 유지로
  아트 가이드 §1.1 충돌을 정리 (ui-ux 1.9, 아트 1.6)
- 다음 할 일: OFL 한글 폰트를 `Art/Fonts/`에 넣고 TMP 에셋 생성 → `SO_UiTheme_Default` →
  화면별 `P_UI_` 프리팹 순으로 19개 뷰 교체

## 2026-08-04 (2)
- 문구 톤: ui-ux §15.5에 버튼 2~6자·명령형·기술 용어 노출 금지·반말 금지를 정의하고,
  오류·위험·감염·접근 불가 문구는 축약 예외로 남김
- 적용: 메인 메뉴·로비 전체와 회의·투표·결과 제목, 로비 복귀 버튼. 투표 버튼은 `기권` →
  `건너뛰기`로 바꾸되 집계·문서 용어는 기권 유지. F10 개발 패널은 내부 도구라 제외
- 다음 할 일: 미션·상호작용 프롬프트 문구 적용. 단 `레시피 확보` 등은
  AntidoteEconomyTests가 검증하므로 테스트와 함께 고쳐야 함

## 2026-08-04 (3)
- 감염 버그: `InfectionService.Tick`을 아무도 호출하지 않아 제한시간이 지나도 죽지 않았다.
  서버 권위 `NetworkInfectionAuthority.Update`에 매 프레임 진행을 연결
- 조명 버그: `Light2D`가 강도×색을 곱하는데 색조가 `#1A2938`(휘도 0.15)로 하드코딩돼
  강도 4%가 실효 0.6%로 눌렸다. 색조를 SO로 올리고 `#7394C7`·강도 0.12(실효 약 7%)로 조정
- 다음 할 일: 실효 암부 밝기를 실제 재생에서 확인하고, 상충 붕괴 기준 0.15에 닿지 않는지 점검

## 2026-08-04 (4)
- GDD 1.6: 손전등·발걸음을 감지 조건에서 제거. 괴물은 감지 반경(5/7/9m) 안이면 무조건 감지하고,
  소음 사건(미션 실패·스피커)이 괴물을 그 방으로 유인하는 구조로 단순화
- 배선 제거: `IsExposedToProximity`, `SetIlluminated`, `SetMovingAudibly`와 서버 발걸음 샘플링을
  삭제. 손전등 복제 값은 원격 연출용으로만 남김. 조명 상한 근거도 스텔스에서 탐색 긴장으로 교체
- 측면 전환: 아트 가이드 §1.1을 오른쪽 측면 프로필로 바꾸고 §2.2 스프라이트 규격 추가,
  `PlayerMotionFeel`에 이동 방향 좌우 플립 연결
- 다음 할 일: 은신이 사라져 8마리·9m 구간이 과도한지 플레이테스트. `F` 소등의 존재 이유 재검토

## 2026-08-04 (5)
- 검증: 배치 모드 EditMode 256개 전부 통과. `ApplyMonsterFlashlightRules` 호출 하나가 남아
  컴파일이 깨져 Play가 막혀 있었고, 그 줄을 제거해 해결
- 낡은 테스트 정리: 씬 검증이 미션 4종 시절 구성(차단기2·CCTV2·시료5)과 수색 3초를 기대하고
  있었다. 실제 SO·빌더·밸런스 문서 기준(9종·시료만 2개, 수색 5초)으로 테스트와 개발 현황 갱신
- 다음 할 일: 에디터에서 실제 재생으로 전역광 실효 밝기, 감염 사망, 좌우 플립 확인

## 2026-08-05
- 캐릭터 교체 준비: 이동 거리 기반 `CharacterWalkAnimator`를 추가해 정지·걷기 4프레임을
  붙일 자리를 만들고, 원숭이에도 좌우 플립을 연결. 걷기 그림 3장이 없으면 빈 배열을 받아
  정지 프레임만 쓰는 기존 동작을 그대로 유지하므로 지금 시점에는 화면이 바뀌지 않는다
- 문서 오류 정정: 아트 가이드 §2.2가 "원숭이 몸통도 회색조"라고 했으나 상태 색은 이마
  RX-9 표식에만 적용된다(§3.1, `FirstPlayableBuilder`가 `RX9Eye`에 틴트). 원숭이 풀컬러로
  바로잡고 파일 이름표·조명 유의·플립 규칙을 §2.2에 추가, 문서 1.7
- 다음 할 일: 8장 PNG 입고 후 프레임 순환·플립 재생 확인. Unity 에디터가 열려 있어
  이번 변경의 컴파일·테스트 검증은 아직 못 함

## 2026-08-05 (2)
- 배경 회색상자 개선: 맵 전체가 흰 정사각형(`S_UnitSquare`) 한 장을 늘린 것이라 방·벽·프롭이
  전부 같은 네모로 보였다. 바닥은 2m 타일 반복, 벽과 프롭 몸체는 9-slice로 바꿔 방 크기가
  달라도 이음새·외곽선 두께가 일정하게 유지되도록 하고, 프롭에 카테고리 표식 9종을 얹었다
- 회귀 주의: 9-slice는 `localScale`을 1로 두므로 벽 콜라이더가 `Vector2.one` 배율에 기대던
  부분을 실제 크기로 바꿨다. 이걸 놓치면 벽이 통과된다. 두께 0.5m 미만 장식은 선으로
  읽혀야 해서 상세 표현에서 제외
- 다음 할 일: 전역광 4% 아래에서 타일·외곽선이 실제로 보이는지 확인 후 아트 투자 규모 판단.
  `EnvironmentPropSlot`을 읽는 교체 리졸버는 여전히 미구현

## 2026-08-05 (3)
- 도구: `Regenerate Procedural Sprites` 메뉴 추가. `EnsureSprite`가 기존 에셋을 그대로
  돌려주는 캐시라 픽셀 함수를 고쳐도 화면이 안 바뀌던 문제를 푼다. 에셋을 새로 만들면
  GUID가 바뀌므로 실행 후 `Build Complete 2D Top Down`을 이어서 돌려야 한다
- 점검: 코드 건강도 확인. `TODO`·`FIXME`·빈 `catch` 0개, 런타임 `FindObject`는 설정 창
  진입 1회뿐. 고칠 나쁜 코드는 없고 미착수 영역이 남은 문제라 개발 현황에 별도 절로 정리
- 다음 할 일: 오디오는 사용자 요청으로 보류하고 문서에만 기록. 사용자 재생 확인 결과
  (배경 시인성·벽 충돌·좌우 플립) 대기

## 2026-08-05 (4)
- 오진 정정: 08-04 (3)에서 "`InfectionService.Tick`을 아무도 호출하지 않는다"고 판단해
  `NetworkInfectionAuthority.Update`에 호출을 넣었으나, `InfectionService.Update`가 이미
  진행하고 있었다. 서버에서만 두 번 감소해 호스트 감염이 두 배 속도로 닳았다. 추가한 호출 제거
- 진짜 원인: 감염 사망이 **표현되지 않는 것**이었다. 로컬 씬에는 `GhostMovementController`가
  없어 `DeadGhost`가 되어도 그대로 걸어다녔고, `InfectionHudView`는 `IsInfected`가 false로
  바뀌는 순간 패널을 지워 아무 일도 없던 것처럼 보였다
- 수정: 로컬 플레이어에 유령 이동을 붙이고 HUD에 `감염 사망 / 유령 — 벽 통과 가능` 표시 추가.
  맵 경계 상수는 `ProjectBootstrap.LaboratoryMapBounds` 하나만 쓰도록 internal로 공개
- 다음 할 일: 미션 9종이 전부 `GUI.Box` 사각형(`FuseMissionView` 1830줄)이라 그래픽화 경로 선택 필요

## 2026-08-05 (5)
- 배경 이야기: 메인 메뉴 첫 진입에 1회 재생하는 `IntroStoryView` 추가. 5장 구성이며 내용은
  새로 만들지 않고 GDD §4 세계관(한도윤 박사·RX-9·정전·여섯 명·15분)을 옮겼다.
  건너뛰기 상시 노출, 자동 넘김 없음, 메뉴의 `이야기` 버튼으로 재생. ui-ux §2.1에 화면 정의 추가
- 확인: 빌런 화면은 이미 구현돼 있다(`NetworkPlayerPresentation`이 5초간 역할 표시,
  `VillainUpgradeHudView`·`SpeakerRemoteView`). 다만 1인 로컬은 생존자 연습 모드로 고정돼
  있어 `HostClient2`로 띄워야 보인다. 감염 버그와 같은 "로컬에는 껍데기만" 패턴
- 다음 할 일: 미션 9종을 uGUI + 절차적 부품 그림으로 이전. 한글 폰트가 없어 TMP 라벨은
  보류하고 도형부터 진행

## 2026-08-06
- 폰트 확보: 에스코어 드림 4·6과 배민 을지로체를 `Art/Fonts/`에 넣고 라이선스 요약을 함께 남김.
  9종 중 Regular·Bold 둘만 넣어 용량을 줄였다. 을지로체는 §15.2대로 로고 전용
- TMP 자동화: `KoreanFontAssetBuilder`가 Dynamic 모드 SDF 에셋을 만들고
  `Create Or Update UI Theme`가 이를 호출해 테마까지 연결한다. Font Asset Creator를 손으로
  돌리는 단계를 없애 사람마다 설정이 갈리지 않게 했다. 한글 11,172자를 미리 굽지 않는다
- 미션 부품: `MissionUiSpriteBuilder`로 패널·버튼·소켓·퓨즈·레버·다이얼·압력계·커넥터·
  시료 접시·상태등 10종을 절차 생성. IMGUI와 uGUI 양쪽에서 그대로 쓰고 손그림 교체도 같은 이름
- 다음 할 일: 미션 9종 화면을 uGUI로 이전. `FuseMissionView` 1830줄이라 한 종씩 진행

## 2026-08-06 (2)
- 씬 생성 복구: `Build Complete 2D Top Down`이 8/3 `cf4e36f` 이후로 계속 실패하고 있었다.
  `SM_InsulatedPallet`이 액체 보관실 East 출입구 통행 구역을, `SM_PackageScanner`가
  실험실 B East 접근 통로를 각각 침범했다. 둘 다 빈 자리로 옮겼다. 검증 코드가 아니라
  배치 좌표 문제였다
- TMP 선행 조건: 프로젝트에 TMP Essential Resources가 임포트된 적이 없어 `TMP_Settings`가
  없었고 폰트 에셋 생성이 그 안에서 죽었다. 자동 임포트를 넣되, 패키지 임포트가 비동기라
  배치에서는 완료 콜백에서 종료하는 별도 진입점을 뒀다
- 배치 검증 통과: 컴파일 에러 0, EditMode 256/256, 씬 생성·검증 통과,
  TMP 폰트 3종·UI 테마·미션 부품 10종 생성 확인

## 2026-08-06 (3)
- 혼합 시점: 벽이 순수 탑뷰의 얇은 선이라 어몽어스식 입체감이 없었다. 카메라를 향한 벽
  (바닥이 아래에 있는 벽)에만 높이 0.95m 정면을 그려 아트 가이드 §1.1의 "바닥은 위에서,
  벽은 정면으로" 혼합 시점을 실제 렌더링에 반영. 카메라는 직교 2D 그대로다
- 정렬: 벽 정면은 바닥 위·프롭 아래(order 2)에 둔다. 방에 서 있는 것들이 항상 벽 앞에
  그려져야 하기 때문이다. 방 아래쪽 벽에는 입면을 그리지 않는다. 그 방 바닥을 덮는다
- 검증: 씬 생성 통과, 벽 정면 26개 생성, EditMode 256/256

## 2026-08-06 (4)
- 미션 화면 정리: 안내문이 방 배너와 겹치고 패널 배경이 반투명이라 글자가 맵과 섞여
  읽히지 않았다. 미션 중에는 방 배너·조작 도움말·조준점·해독제 상태를 숨기고 패널을
  불투명으로 바꿨다. 사건 배너와 위험·피격 표시는 남긴다. 괴물 접근을 놓치면 안 된다
- 압력 밸브: 흰 사선 막대 대신 다이얼 스프라이트를 실제로 회전시킨다. 숫자는 회전 밖에서
  그려 읽히게 분리했다. 조작 도움말은 고정 폭 610px이라 잘려 항목이 붙어 보였다. 실제 폭 측정으로 교체
- 구조: 부품 그림 참조를 `SO_MissionUiSprites_Default` 카탈로그 한 곳에 모으고
  (project-structure §8), 미션이 화면을 덮었는지 알리는 `MissionOverlayState`를 뒀다.
  카탈로그가 비면 예전 회색 상자로 되돌아가 그림이 없어도 깨지지 않는다
- 검증: 컴파일 에러 0, 씬 생성 통과, EditMode 256/256

## 2026-08-06 (5)
- 상호작용 인지: 미션 스테이션이 배경 프롭과 구분되지 않았다. 본체보다 조금 큰 판을 뒤에 깔고
  플레이어가 다가올수록 여백을 0.05m에서 0.24m로 넓혀 테두리가 실제로 두꺼워지게 했다.
  9-slice 링을 키우면 굵기는 그대로인 채 밖으로 밀려나기만 해서 쓰지 않았다
- 소유자 전용: 강조 드라이버를 네트워크 플레이어의 소유자 전용 목록에 넣었다. 원격
  플레이어가 다가갈 때 내 화면의 설치물이 켜지면 안 된다. 강조는 조명 영향을 받지 않는
  재질을 써서 어두운 방에서도 보인다
- 검증: 컴파일 에러 0, 씬 생성 통과, EditMode 256/256

## 2026-08-06 (6)
- 시점 시범: 캐릭터와 벽은 서 있는데 프롭만 눕어 있어 괴리가 있었다. 액체 보관실
  프롭만 footprint를 발밑으로 보고 세워 그린다(높이 = 깊이×0.55 + 0.85m). 발밑 그림자로
  접지면을 남긴다. `ElevationPilotRoomId` 상수 하나로 적용 범위를 바꾼다
- Y 정렬: 세우면 서로 겹치므로 고정 정렬로는 앞뒤가 뒤집힌다. 발이 닿는 Y 기준으로
  정렬 순서를 계산하는 `YSortedRenderer`를 넣고 캐릭터도 같은 대역에 올렸다.
  캐릭터는 발이 몸통 중심보다 0.6m 아래라 그만큼 보정
- 검증: 컴파일 에러 0, 씬 생성 통과, EditMode 256/256

## 2026-08-06 (7)
- 폰트 반영: 화면 17개를 uGUI로 옮겨야 한글 서체가 보인다고 판단한 것이 틀렸다. IMGUI도
  `GUI.skin.font`로 서체를 바꿀 수 있다. `ImguiFontInstaller`가 어떤 씬에서 시작하든
  자동 생성돼 기본 서체를 에스코어 드림으로 바꾼다. TMP 에셋과 같은 파일을 가리키므로
  나중에 uGUI로 옮겨도 글자 모양이 달라지지 않는다
- 배치: 서체 파일은 옮기지 않고 이를 가리키는 `SO_ImguiFontSet`만 Resources에 둔다
- 검증: 에디터 컴파일 에러 0, 폰트 세트 생성·연결 확인

## 2026-08-06 (8)
- 바닥 아트: RX-9 실험실 방·복도용 512px 반복 타일 2종을 제작했다. 방은 밝은 의료 패널,
  복도는 체결식 금속 패널로 분리하고 기존 방별 곱셈 색상을 유지하도록 회색조로 보정했다
- 적용: 실험실 씬의 방 10개·복도 22개를 새 타일로 교체했다. `FirstPlayableBuilder`도 완성
  PNG를 우선 사용하고 누락 시 기존 절차형 회색상자로 복귀한다. 변환 도구와 원본도 보관한다
- 검증: 진행 1회차라 Unity 테스트는 생략. 해상도와 상하·좌우 반복 경계 일치를 확인했다

## 2026-08-06 (9)
- 벽 아트: RX-9 모듈형 벽 단면·정면 2종을 제작했다. 단면은 어두운 캡과 청록 체결부,
  정면은 밝은 상단 립·청회색 패널·검은 걸레받이로 혼합 시점을 분명히 했다
- 적용: 실험실 씬의 벽 단면 108개·정면 26개를 64px 테두리 9-slice PNG로 교체했다.
  빌더는 완성 PNG 우선·기존 절차형 벽 폴백으로 동작한다
- 검증: 진행 2회차라 Unity 테스트는 생략. 0.32m 가로·세로 단면과 0.95m 정면의
  9-slice 확대 미리보기에서 모서리·외곽선 유지 여부를 확인했다

## 2026-08-06 (10)
- 자동문 아트: RX-9 의료 백색 압력문 패널과 어두운 구조 문틀을 제작했다. 둘 다
  256px·512 PPU·사방 64px 9-slice라 가로문과 세로문이 같은 에셋을 사용한다
- 적용: 고유 출입구 27개의 패널 54개·문틀 54개를 완성 PNG로 교체했다. 상태등은 기존
  Unlit 렌더러를 유지해 닫힘·이동·열림 색이 계속 바뀐다. PNG 누락 시 회색상자로 복귀한다
- 검증: Unity 6 임포터 설정 방식을 수정한 뒤 컴파일 오류 0, 씬 생성·First Playable 검증
  통과, 새 문 참조 수 확인, EditMode 256/256 통과

## 2026-08-06 (11)
- 길찾기 아트: RX-9 방·출입구 공용 이름판과 4방향 바닥 유도 데칼을 제작했다. 이름판의
  한글은 이미지에 굽지 않고 커밋된 `SCDream6` 서체로 동적 표시한다
- 적용: 방 10개와 고유 출입구 27개에 이름판을 붙이고 복도 유도 데칼은 경로 방향에 맞춰
  회전·좌우 반전한다. 완성 PNG가 없으면 기존 절차형 표지와 유도등으로 복귀한다
- 검증: Unity 씬 생성·First Playable 검증 통과. 9-slice 두 크기와 데칼 4방향 검수표를
  확인했고 진행 4회차라 테스트 메뉴는 생략했다

## 2026-08-06 (12)
- 조명 아트: RX-9 천장 조명 패널과 비상 경고등을 투명 스프라이트로 제작했다. 의료 백색·
  청록 조명과 주황 경고 렌즈가 기존 벽·문·표지판과 같은 구조재를 공유한다
- 적용: 방 10개에 천장등 20개와 경고등 10개를 배치했다. Unlit 외형만 추가하고 실제
  `Light2D` 수치는 바꾸지 않아 기존 어둠·손전등 밸런스를 유지한다
- 검증: Unity 씬 생성·First Playable 검증 통과. 투명 모서리·해상도와 배치 검수표를
  확인했고 진행 5회차라 테스트 메뉴는 생략했다

## 2026-08-06 (13)
- 공용 설비 아트: RX-9 벽 모니터·소화기·쓰레기통·비상전화 4종을 투명 스프라이트로 제작했다
- 적용: 방 10개에 설비를 각 1개씩 총 40개 배치하고, 벽면·바닥 설치 종류를 구분했다. PNG 누락 시 절차형 표시로 복귀한다
- 검증: Unity 씬 생성·First Playable 검증 통과, 설비 참조 각 10개 확인, EditMode 256/256 통과

## 2026-08-06 (14)
- 방별 가구 아트: RX-9 연구 작업대·보관 캐비닛·시약 선반·이동 카트 4종을 투명 스프라이트로 제작했다
- 적용: 백신실·실험실·보관실·전력실·입원실의 16개 슬롯을 완성 PNG로 교체하고 기존 위치·충돌체·접지 그림자를 유지했다
- 검증: Unity 씬 생성·First Playable 검증 통과, 작업대 4·캐비닛 3·선반 6·카트 3개 참조 확인. 진행 7회차라 전체 테스트는 생략했다

## 2026-08-06 (15)
- 실험 장비 아트: RX-9 원심분리기·현미경·약품 냉장고·생물안전작업대 4종을 투명 스프라이트로 제작했다
- 적용: 백신실 A·B와 실험실 A·B의 7개 장비 슬롯을 완성 PNG로 교체하고 기존 위치·충돌체·접지 그림자를 유지했다
- 검증: Unity 씬 생성·First Playable 검증 통과, 원심분리기 2·현미경 1·냉장고 2·안전작업대 2개 참조 확인. 진행 8회차라 전체 테스트는 생략했다

## 2026-08-06 (16)
- 원숭이 평상 감지를 1.25/1.75/2.25m, 물기 표면 간격을 0.2m로 축소하고 밸런스 SO·관련 문서를 동기화했다
- 미션 실패는 5.33m, 빌런 스피커는 8m 급습 반경으로 현장 도착 후 10초간 소음원 주변을 빠르게 배회하도록 구현했다
- 검증: Gameplay·EditMode 테스트 어셈블리 독립 컴파일 통과. Mac 잠금으로 Unity EditMode 실행은 잠금 해제 후 진행 필요

## 2026-08-06 (17)
- 보안실 장비 아트: RX-9 서버 랙·CCTV 모니터 월·전자지도 테이블·운영 콘솔 4종을 투명 스프라이트로 제작했다
- 적용: 보안실의 서버 랙 2·CCTV 월 1·전자지도 1·운영 콘솔 2개 슬롯을 완성 PNG로 교체하고 Unlit 외형과 기존 충돌체를 유지했다
- 검증: 4종 알파·해상도와 배치 검수표 확인, Editor 어셈블리 독립 컴파일 통과. Mac 잠금으로 Unity 전체 테스트는 보류했다

## 2026-08-06 (18)
- 전력실 장비 아트: RX-9 발전기·차단기 뱅크·케이블 릴·예비 전지 랙 4종을 투명 스프라이트로 제작했다
- 적용: 전력실의 발전기 1·차단기 1·케이블 릴 2·전지 랙 1개 슬롯을 완성 PNG로 교체하고 기존 위치·충돌체·상태등을 유지했다
- 검증: 4종 알파·해상도와 배치 검수표 확인, Editor 어셈블리 독립 컴파일로 참조와 문법을 확인했다

## 2026-08-06 (19)
- 입원실 장비 아트: RX-9 병상·커튼 레일·링거 스탠드·환자 모니터 4종을 투명 스프라이트로 제작했다
- 적용: 입원실의 병상 4·레일 2·링거 2·모니터 2개 슬롯을 완성 PNG로 교체하고 기존 위치·충돌체를 유지했다
- 검증: 4종 알파·해상도와 배치 검수표를 확인하고 Editor 어셈블리를 독립 컴파일했다. 다음 진행 회차에 전체 테스트를 수행한다

## 2026-08-06 (20)
- 입원실 보조 장비 아트: RX-9 약품 카트·간호 스테이션·산소 포트·약품 캐비닛 4종을 투명 스프라이트로 제작했다
- 적용: 입원실 카트 1·스테이션 1·산소 포트 2·캐비닛 1개 슬롯을 완성 PNG로 교체하고 기존 위치·충돌체·상태등을 유지했다
- 검증: 에셋 정적 검증·Editor 컴파일·Complete 2D 씬 검증 통과, Unity EditMode 257/257 통과

## 2026-08-06 (21)
- 입원실 바닥 연출: 서로 다른 끌림·웅덩이 혈흔 2종과 정확한 `1 → 2 → 3` 트리아지 번호 데칼을 제작했다
- 적용: 입원실 VFX 3개 슬롯을 완성 PNG로 교체하고 혈흔도 `FloorDecal`로 정리해 장비보다 아래에 표시한다
- 검증: 알파·해상도·배치 검수표, Editor 컴파일, Complete 2D 씬 검증 통과. 진행 1회차라 전체 테스트는 생략했다

## 2026-08-06 (22)
- 격리실 핵심 장비 아트: RX-9 광폭 강화 셀·단일 셀·케이지 포드·제독 유닛 4종을 투명 스프라이트로 제작했다
- 적용: 격리실 A·B의 광폭 셀 1·단일 셀 3·포드 2·제독 유닛 2개 슬롯을 교체하고 기존 위치·충돌체를 유지했다
- 검증: 알파·해상도·배치 검수표, Editor 컴파일, Complete 2D 씬 검증 통과. 진행 2회차라 전체 테스트는 생략했다

## 2026-08-06 (23)
- 격리실 보조 장비 아트: RX-9 잠금장치·관찰 콘솔·제독 샤워·억제 레일·억제 제어기 5종을 투명 스프라이트로 제작했다
- 적용: 격리실 A·B의 잠금장치 2·콘솔 2·샤워 2·레일 1·제어기 1개 슬롯을 교체하고 기존 위치·충돌체·상태등을 유지했다
- 검증: 알파·해상도·배치 검수표, Editor 컴파일, Complete 2D 씬 검증 및 Unity EditMode 257/257 통과

## 2026-08-06 (24)
- 격리실 연출 아트: RX-9 경고등·봉쇄 바닥 격자·깨진 강화유리·정확한 `01 → 02 → 03` 구역 번호 4종을 제작했다
- 적용: 격리실 A·B의 경고 슬롯 3·바닥 격자 1·파손 흔적 1·구역 번호 1개를 완성 PNG로 교체했다
- 검증: 알파·해상도·배치 검수표, Editor 컴파일과 Complete 2D 씬 검증을 통과했다. 새 주기 1회차라 전체 테스트는 생략했다

## 2026-08-06 (25)
- 액체 보관실 핵심 장비 아트: RX-9 극저온 탱크·시료 드럼·결빙 배관·온도 단말기·냉각수 매니폴드 5종을 제작했다
- 적용: 극저온 탱크 3개와 나머지 장비 각 1개, 총 7개 슬롯을 완성 PNG로 교체하고 기존 충돌체·상태등을 유지했다
- 검증: 알파·해상도·배치 검수표, Editor 컴파일과 Complete 2D 씬 검증을 통과했다. 새 주기 2회차라 전체 테스트는 생략했다

## 2026-08-06 (26)
- 액체 보관실 완성·백신실 시작 아트: 냉장 선반·단열 팔레트·서리 배수구·바이알 랙·무균 작업대 5종을 제작했다
- 적용: 액체 보관실 4개와 백신실 A·B 3개, 총 7개 슬롯을 완성 PNG로 교체하고 기존 충돌체·바닥 데칼 종류를 유지했다
- 검증: 알파·해상도·배치 검수표, Editor 컴파일과 Complete 2D 씬 검증을 통과했다. 변경된 5회 주기의 3회차라 전체 테스트는 생략했다

## 2026-08-06 (27)
- 백신실 보조 장비 아트: 제독 세척대·PPE 디스펜서·무균 바닥 구역·주입기 시험기·혼합 작업대 5종을 제작했다
- 적용: 백신실 A·B의 공유 설비 6개와 B 전용 설비 2개, 총 8개 슬롯을 완성 PNG로 교체했다
- 검증: 알파·해상도·배치 검수표, Editor 컴파일과 Complete 2D 씬 검증을 통과했다. 5회 주기의 4회차라 전체 테스트는 생략했다

## 2026-08-06 (28)
- 실험실 A 전용 설비 아트: 시료 랙·공조 배출구·검체 스캐너·비상 세안기·천장 서비스 레일 5종을 제작했다
- 적용: 실험실 A 5개와 실험실 B 공유 배출구 1개, 총 6개 슬롯을 완성 PNG로 교체했다
- 검증: 알파·해상도·배치 검수표, Editor 컴파일, Complete 2D 씬 검증과 Unity EditMode 257/257을 통과했다

## 2026-08-06 (29)
- 실험실 A·B 아트: 화학 유출 데칼과 백업 서버 랙·시료 밀봉기·패키지 스캐너·밀폐 상자 더미 5종을 제작했다
- 적용: 실험실 A 마지막 데칼 1개와 실험실 B 핵심 설비 4개를 완성 PNG로 교체했다
- 검증: 알파·해상도·배치 검수표, Editor 컴파일과 Complete 2D 씬 검증을 통과했다. 새 5회 주기의 1회차라 전체 테스트는 생략했다

## 2026-08-06 (30)
- 역할 공개를 HUD 최상단 전체 화면 연출로 확대하고 시민·빌런 목표, 비밀 안내, 자동 종료와 확인 버튼을 추가했다
- 긴급회의 진입 버튼에 투표 채팅 시작, 보호 시간·쿨타임·남은 횟수를 명시하고 토론 후 자동 투표 안내를 추가했다
- 기존 서버 권위 회의·채팅 규칙은 유지했으며 요청에 따라 Unity 실행과 테스트는 생략했다

## 2026-08-06 (31)
- Tab 지도가 플레이어 입력 연결 시점에 의존하지 않도록 키보드 직접 입력 안전망과 프레임 중복 방지를 추가했다
- 0%부터 현재 위치와 연구소 공간 배치도를 표시하고, 50% 전에는 미확인 구역 이름만 숨겨 기존 해금 규칙을 유지했다
- 요청에 따라 Unity 실행과 테스트는 생략했다

## 2026-08-06 (32)
- 새 라운드마다 같은 역할이 재배정돼도 시민·빌런 전체 화면 역할 공개가 반드시 다시 시작되도록 수명주기를 보강했다
- 시민 HUD에 25/50/75/100% 단계별 현재 보상과 다음 해금을 표시해 조명·보안·출구 활성화를 즉시 확인할 수 있게 했다
- 빌런 HUD에 후각 탐지 반경, 원숭이 수, 감염 제한시간의 실제 단계 수치를 표시했으며 기존 서버 강화 적용 규칙은 유지했다

## 2026-08-06 (33)
- 평상 감지 1.25/1.75/2.25m를 중심점이 아닌 콜라이더 표면 간 거리로 판정해 가까워도 감지할 수 없던 문제를 수정했다
- 물기 표면 거리 0.2m와 미션 실패 5.33m·스피커 8m 급습 반경은 기존 요청값을 유지했다
- Presentation의 잘못된 Input System 직접 참조를 제거하고 Tab 지도 입력을 로컬 플레이어 바인딩 초기에 연결했다

## 2026-08-06 (34)
- 역할 공개를 정적 안내 상자에서 캐릭터 외형·역할 색·확대와 페이드가 포함된 전체 화면 시네마틱 카드로 교체했다
- 긴급회의 전환·대형 채팅과 전체 화면 작전 지도·미션 사이드 패널을 적용해 핵심 정보 화면을 게임형 레이아웃으로 교체했다
- 역할 비밀, 토론 후 자동 투표 등 기존 서버 규칙과 정보 공개 범위는 유지했다

## 2026-08-06 (35)
- Default 씬 직접 실행은 역할·회의 없는 1인 연습, HostClient2 로비 시작은 전체 네트워크 게임임을 실행 문서에 명시했다
- 로컬 연습 플레이어에도 Tab 입력을 연결하고 현재 위치를 네트워크 플레이어 대신 로컬 플레이어에서 읽도록 수정했다
- 원숭이는 두 모드 모두 30초 시작 보호 이후 공격하며 평상 감지는 수정된 콜라이더 표면 거리 판정을 사용한다

## 2026-08-06 (36)
- 서버가 소유자에게 배정한 개인 미션 4~5개를 플레이 중 좌측 HUD에 위치·완료 상태와 함께 항상 표시하고 접기 기능을 추가했다
- Tab 전체 지도에 미완료 개인 미션을 목록과 같은 번호로 표시해 첫 동선과 목표 위치를 즉시 확인할 수 있게 했다
- 검증: Unity Editor Presentation 어셈블리 컴파일 성공. 소유자 전용 목록과 빌런 위장 목록 규칙을 유지해 남의 미션은 공개하지 않는다

## 2026-08-06 (37)
- 공통 회색조 바닥 타일은 유지하면서 백신·실험·격리 A/B를 포함한 10개 방에 기능별 고유 저채도 색조를 적용했다
- 복도는 중립 청회색으로 유지하고 방 바닥만 한 단계 밝게 해 정전 분위기와 방 경계 가독성을 함께 유지했다
- 현재 씬·재생성 빌더를 함께 수정하고 EditMode에 10개 바닥색 중복 방지 검증을 추가했다

## 2026-08-06 (38)
- 예전 맵 기준 유령 경계 `x -52~67, y -40~36`을 실제 연구소 바닥 외곽 2m인 `x -42.5~38, y -25~32`로 축소했다
- PlayerMotor의 이동 목표와 GhostMovementController의 물리 후 위치를 이중 제한해 실행 순서와 관계없이 결계를 넘지 못하게 했다
- 로컬 씬·네트워크 프리팹·재생성 빌더·유령 경계 EditMode 테스트를 같은 좌표로 동기화했다

## 2026-08-06 (39)
- HostClient 시작 씬 강제 지정은 Unity의 기본 Scenario 수명주기와 충돌해 제거하고 시나리오의 Initial Scene 설정을 사용하도록 복구했다.
- HostClient2의 오래된 가상 프로젝트 캐시는 `/private/tmp`으로 옮겨 다음 실행에서 깨끗하게 재생성되도록 했다.

## 2026-08-06 (40)
- Additional Editor 방식에는 불필요한 로컬 배포 플래그 변경을 되돌리고 Unity 6.3 기본 HostClient2 설정을 복구했다.
- 비호스트에서도 회의 참가자와 투표 후보 전원이 보이도록 공개 슬롯 복제 정보를 사용하고, 생존자 전용 채팅·최종 개별 투표 공개를 서버 권위로 보강했다.
- Scenario 에셋 선택 시 PreviewImporter 워커에서 발생하는 `GetAllInstances` null assertion을 차단하고 한 번에 구성·실행하는 메뉴를 추가했다.

## 2026-08-06 (41)
- 빈 가상 프로젝트 식별자와 사라진 VP 디렉터리를 시작 전에 자동 복구해 HostClient2 첫 실행의 `project directory for []` 실패를 제거했다.
- Scenario의 Editor 노드를 `run`·`deploy` 개별 ID로 구성하고 MPPM 한계에 맞는 최대 4-Editor 설정만 만들도록 수정했다.
- 검증: Unity 컴파일 성공, Main Editor·Player 2 동시 활성화와 Bootstrap 로드 확인, 새 MPPM 경고·Assertion·C# 오류 없음.

## 2026-08-06 (42)
- HostClient 실행 중 Standard 설정으로 전환하던 처리를 제거해 Main Editor 실행 노드 중단과 Player 2 검은 화면을 수정했다.
- 중복 시작 방지와 공식 `PlayModeManager.Stop()` 기반 종료 메뉴를 추가하고, 종료 뒤 재실행되던 영구 예약을 제거했다.
- 검증: Main Editor·Player 2 모두 Bootstrap에서 MainMenu 진입, 오류 없는 종료와 40초 후 미재실행을 확인했다.

## 2026-08-06 (43)
- 맵 개편 전 좌표를 사용하던 네트워크 플레이어 스폰 배열을 현재 연구소 씬의 `PlayerSpawn_01~06` 내부 마커와 동기화했다.
- Player 2 스폰을 바깥 공간 `(-10, 24)`에서 실험실 내부 `(-17, 15)`로 옮기고, Bootstrap 재생성 좌표와 EditMode 기대값도 함께 갱신했다.
- 검증: Unity 스크립트 컴파일 성공, 런타임·빌더·씬의 6개 스폰 좌표 일치 및 이전 좌표 잔존 없음 확인.

## 2026-08-06 (44)
- 실제 `10_Laboratory` 전체 씬 캡처의 방·복도 구조를 유지한 1495×1052 정적 작전 지도 이미지를 제작·적용했다.
- Tab 지도의 절차형 방 상자·미확인 구역·미션 번호를 제거하고 고정 한글 방 이름과 본인 위치 점 하나만 표시하도록 교체했다.
- 위치 점은 10개 방 중심 보정과 삼각형 보간으로 월드 좌표를 이미지 UV에 대응하며 지도 텍스처 존재 검증을 추가했다.

## 2026-08-06 (45)
- 직교 카메라·게임 좌표는 유지하고 벽 정면 높이, 바닥형 가구 높이와 납작한 접지 그림자를 전체 10개 방에 적용했다.
- 플레이어·원숭이·가구를 발 Y 기준으로 정렬하고 문 패널·문틀 전면을 강조해 2.5D 혼합 시점을 통일했다.
- 현재 씬 런타임 보정과 재생성 빌더를 같은 상수로 연결하고 벽 상단·가구 접지점 불변 EditMode 검증을 추가했다.
