# 아트·오디오 에셋 가이드

> 문서 버전: 1.6
> 기준 GDD: 1.1
> 목표: 작은 팀이 일관된 2D 탑다운 연구소를 빠르게 제작하기 위한 아트 바이블과 에셋 목록

---

## 1. 시각 방향

### 1.1 스타일

- 스타일라이즈드 2D 탑다운 일러스트
- 단순한 형태, 굵은 외곽선과 읽기 쉬운 실루엣
- 사실적인 재질보다 방 기능과 위험의 가독성 우선
- 직접적 고어보다 혈흔, 붉은 경고등, 그림자와 소리로 공포 표현
- 바닥과 방 배치는 위에서 내려다본 평면으로 표현한다
- **캐릭터는 정수리가 아니라 오른쪽을 보는 측면 프로필로 그린다.** 카메라는 직교·비회전이고
  몸통도 회전하지 않으므로, 위에서 본 형태보다 측면 실루엣이 훨씬 잘 읽힌다. 왼쪽 방향은
  좌우 플립으로 처리하므로 한 방향만 그린다
- 바닥은 위에서, 캐릭터와 벽 설비는 정면·측면으로 그리는 혼합 시점을 의도적으로 쓴다.
  카툰 소셜 디덕션 게임의 관례이며 원근 왜곡을 흉내 내지 않는다
- 다른 소셜 디덕션 게임의 캐릭터·방·아이콘·로고를 복제하지 않고, RX-9 연구소 고유의
  방호복·원숭이 실루엣과 색 체계를 사용. 단 UI의 **형태 문법**(둥근 두꺼운 버튼,
  굵은 외곽선 글자, 하이컨트라스트 플랫 패널)은 카툰 소셜 디덕션 게임의 가독성
  관행을 따른다 (`ui-ux-design.md` §15.3). 복제하지 않는 것은 고유 자산이고,
  따르는 것은 가독성 관행이다
- 직교 카메라 거리에서 보이지 않는 작은 디테일은 제작하지 않음

### 1.2 핵심 대비

- 차가운 청록·청색 연구소 환경
- 따뜻한 주황 비상등
- 빌런 강화와 감염은 강한 적색
- 상호작용 가능 오브젝트는 약한 청록 발광

### 1.3 권장 팔레트

| 용도 | 색상 예시 |
| --- | --- |
| 배경 암부 | `#111820` |
| 금속 벽 | `#35434C` |
| 의료 백색 | `#C8D4D8` |
| 시스템 청록 | `#34C6C8` |
| 전력 노랑 | `#E7B84B` |
| 경고 주황 | `#E87532` |
| 감염 적색 | `#D63B42` |
| 빌런 자주 | `#8C4EC7` |
| UI 본문 백색 | `#F2F6FA` |
| UI 패널 배경 | `#141A26` |
| UI 패널 테두리 | `#3A4A63` |
| UI 글자 외곽선 | `#05080D` |
| UI 유령 청회색 | `#8FA6B8` |

색상은 방향이며 최종 머티리얼과 조명에서 명도 대비를 확인한다. UI는 위 표만 쓰고
독자 팔레트를 만들지 않는다. `의료 백색`은 월드 재질용이며 UI 본문에는 더 밝은
`UI 본문 백색`을 쓴다.

---

## 2. 캐릭터 에셋

### 2.1 직원

MVP는 기본 바디 한 개를 공유한다.

- 실험복 또는 회사 작업복 실루엣, 오른쪽을 보는 측면 프로필
- 멀리서도 번호가 보이는 등·가슴 표식
- 손전등 장착 지점

**몸통은 회색조로 그린다.** 플레이어 색 6종은 런타임에
`SpriteRenderer.color` 틴트로 곱해 넣으므로, 색을 칠하면 로비 색상이 먹지 않는다.
흰색을 기준으로 음영만 회색으로 넣는다. 바이저는 틴트를 받지 않아야 하므로 별도
스프라이트 레이어로 분리한다.

### 2.2 캐릭터 스프라이트 규격

| 항목 | 값 |
| --- | --- |
| 캔버스 | 1254 × 1254 px 정사각, 배경 투명, PNG RGBA 8bit |
| Pixels Per Unit | 1024 (1254px ≈ 월드 1.22m) |
| 피벗 | Center. 캐릭터를 캔버스 정중앙에 배치 |
| 방향 | 오른쪽 바라보기 한 방향만 |
| 프레임 | 정지 1장 + 걷기 4장 |
| 비율 | 키:폭 약 1:0.75. 캔버스를 꽉 채우지 않고 여백을 둔다 |

걷기 4프레임은 접지A → 모음 → 접지B → 모음 순환이고, 모음 프레임을 두 번 재사용하므로
실제로 그리는 그림은 **정지·접지A·모음·접지B 4장**이다. 상하 바운스는
`PlayerMotionFeel`이 절차적으로 만들므로 프레임에 그려 넣지 않는다.

원숭이도 같은 규격이며, 상태(순찰·추격·물기)를 틴트로 구분하므로 **원숭이 몸통도 회색조**로 그린다.
- 배터리 운반 소켓
- 해독제 사용 손 소켓
- 유령용 단순 반투명 버전

현재 프로토타입 본체는 `S_Player_Survivor.png`를 사용하고 흰 방호복 영역에
런타임 플레이어 색상을 곱한다. 검은 바이저와 장비 윤곽은 색상 변경 후에도 유지한다.
단일 방향 프로토타입에서는 본체를 화면 기준으로 세워 두고 `AimPivot`과 손전등만 조준
방향으로 회전한다. 4방향 프레임을 적용하기 전까지 본체 스프라이트 자체를 회전하지 않는다.

#### 초기 스프라이트 목표

| 항목 | 기준 |
| --- | --- |
| 본체 프레임 | 기준 256×320px, 동일 픽셀 밀도 유지 |
| 방향 | MVP 4방향, 제작 여유가 있으면 8방향 |
| 색상 | 흰색 마스크 본체 1세트에 런타임 색상 6종 적용 |
| 레이어 | 몸, 바이저·얼굴, 장착 아이템, 상태 효과 분리 |
| 아틀라스 | 플레이어 공용 2048px 이하 1장 권장 |

### 2.2 직원 애니메이션

- Idle
- Walk
- Run
- Turn 또는 방향 블렌딩
- InteractShort
- InteractLoop
- CarryIdle
- CarryWalk
- Bitten
- UseAntidote
- Death
- GhostIdle/Move는 일반 이동 재사용 가능

Root Motion은 사용하지 않고 코드 이동과 2D Animator 파라미터를 사용한다.

---

## 3. 원숭이 괴물

### 3.1 형태

- 일반 원숭이보다 길고 불균형한 팔
- 낮은 자세에서도 탑뷰 실루엣이 직원과 명확히 다름
- 붉은 눈 또는 목 주변 RX-9 흔적
- 직접적인 장기 노출보다 변형된 피부와 털 표현
- 단계가 올라가도 모델 교체보다 머티리얼·VFX 강도 변화 사용

현재 프로토타입 본체는 `S_Monkey_Mutant.png`를 사용한다. AI 상태 색은 몸 전체를
바꾸지 않고 이마의 RX-9 표시만 바꿔 원숭이 외형과 상태 정보를 분리한다.
단일 방향 프로토타입의 몸은 화면 기준으로 고정하고, 이동·시야·물기에는 별도의 논리 방향
벡터를 사용한다. 이후 4방향 프레임이 준비되면 논리 방향으로 프레임을 전환한다.

### 3.2 기술 기준

| 항목 | 기준 |
| --- | --- |
| 본체 프레임 | 기준 384×384px |
| 방향 | MVP 4방향, 제작 여유가 있으면 8방향 |
| 레이어 | 몸, 눈·RX-9 발광, 상태 효과 분리 |
| 아틀라스 | 괴물 공용 2048px 이하 1장 권장 |
| 콜라이더 | `CapsuleCollider2D` 이동 영역 + 서버 물기 거리 판정 |

### 3.3 애니메이션

- Idle
- Walk
- FastRun
- Listen
- Search
- Turn
- Bite
- Recover
- CageExit

물기 애니메이션은 판정 프레임을 Animation Event 자체로 결정하지 않고 서버 타이밍 데이터와 맞춘다.

---

## 4. 모듈형 2D 환경 키트

### 4.1 구조물

- 1m, 2m, 4m 벽 단면 스프라이트
- 안쪽·바깥쪽 코너와 문틀
- 유리벽·격리실 강화 유리 오버레이
- 바닥 타일 2m, 4m와 방별 데칼
- 낮은 기둥과 반투명 상부 장식
- 환풍구, 파이프, 케이블 트레이
- 자동문·격리문 애니메이션
- 비상구·옥상문 표식

피벗과 그리드는 1m 단위를 기본으로 통일한다.

### 4.2 조명

- 일반 형광등
- 비상 적색등
- 바닥 유도등
- 방 표지판 발광
- CCTV 상태등
- 스피커 LED
- 제작기 상태등

조명 스프라이트와 2D 발광·VFX를 분리해 품질 프리셋에서 동적 효과를 줄일 수 있게 한다.

---

## 5. 방별 핵심 프롭

### 공용

- 책상, 의자, 캐비닛, 쓰레기통
- 연구 서류, 클립보드, 박스
- 벽 모니터, 키패드, 경고 표지
- 소화기, 비상 전화, 배선함
- 카트, 바닥 케이블, 작은 파티션

### 백신실

- 해독제 제작기
- 약품 냉장고
- 주사기와 바이알
- 지정 보관함
- 독성 강화 단말

### 실험실

- 실험대
- 원심분리기
- 현미경
- 시료 랙
- 약품 병
- 환풍구와 붉은 연기 배출구

### 보안실

- 서버 랙
- CCTV 모니터 월
- 전자지도 테이블
- 로그 단말기
- 격리 제어 패널

### 격리실

- 우리 또는 강화 유리 셀
- 대형 잠금장치
- 경고등
- 파손된 문 변형
- 괴물 생성 지점 마커

### 전력실

- 퓨즈 박스
- 차단기 패널
- 발전기
- 비상 배터리
- 케이블 릴

### 액체 보관실

- 냉장 탱크
- 대형 시료통
- 냉장 선반
- 결빙 파이프
- 레시피 서류 후보

### 입원실

- 침대
- 커튼 레일
- 수액대
- 의료 모니터
- 약품 카트
- 혈흔 데칼

현재 회색상자 프롭은 모두 `EnvironmentPropSlot`에 `roomId`, `assetKey`, `footprint`,
`isObstacle`을 저장한다. 최종 에셋은 `PROP_<Room>_<AssetKey>_<Index>` 루트의
`PlaceholderVisual`을 교체하되 루트 위치와 충돌 풋프린트는 유지한다. 자동문은
`P_AutomaticDoor`, 복도 유도등은 `VFX_FloorGuideLight` 슬롯을 사용한다.

---

## 6. 스프라이트와 머티리얼

- URP의 Sprite 호환 셰이더를 기본으로 한다.
- 화면 밖의 투명 픽셀을 과도하게 포함하지 않는다.
- 환경은 바닥·벽·프롭 단위 Sprite Atlas를 공유한다.
- 캐릭터는 흰색 색상 마스크를 기반으로 6색을 런타임 적용한다.
- 방 구분은 별도 구조보다 바닥 색, 표지, 데칼로 해결한다.
- 픽셀 밀도는 월드 1m당 64~100px 범위에서 한 값으로 통일한다.
- 원본 작업 파일과 Unity용 PNG 출력 파일을 분리한다.

### 권장 출력

- 스프라이트: 투명 PNG
- 색상 마스크·UI: sRGB import
- 마스크·노멀맵: 용도에 맞춰 Linear import
- 오디오 원본: WAV
- 런타임 압축: Unity Import Settings에서 용도별 적용

---

## 7. VFX 목록

| 우선순위 | VFX | 목적 |
| --- | --- | --- |
| P0 | 손전등 빛과 먼지 | 기본 시야 |
| P0 | 전기 스파크 | 미션 실패와 소음 원인 |
| P0 | 붉은 환풍구 연기 | 후각 강화 단서 |
| P0 | 감염 비네팅·혈관 | 개인 위험 |
| P0 | 해독제 사용 | 치료 성공 |
| P0 | 격리문 경고·개방 | 추가 괴물 예고 |
| P0 | RX-9 최종 가스 | 시간 초과 엔딩 |
| P1 | 제작기 진행 증기 | 제작 상태 |
| P1 | 조명 복구 파동 | 프로젝트 단계 |
| P1 | 유령 외곽선 | 사망 후 상태 |
| P2 | 헬기 탐조등 | 승리 연출 |

VFX는 단서를 가리거나 실제 소음 반경보다 큰 위험 범위를 암시하지 않아야 한다.

---

## 8. 조명과 후처리

### 8.1 조명

- 환경 기본광으로 완전한 검정을 피한다.
- 정적 환경은 베이크 조명 또는 혼합 조명을 우선한다.
- 손전등, 비상등과 강화 단서는 동적 조명을 제한적으로 사용한다.
- 캐릭터 얼굴보다 실루엣과 바닥 이동 경로가 우선이다.
- 회색상자 월드는 URP 2D Lit 재질을 사용하며 0%에는 3.5% 비상광만 전역에 남긴다.
- 플레이어 주변 최소 시야와 54도 손전등 밖에서는 오브젝트 색을 식별할 수 없어야 한다.
- 유도 표식·자동문 상태등·미션 비콘은 작은 Unlit 발광부만 사용하고 넓은 바닥을 밝히지 않는다.

### 8.2 후처리

- 약한 Bloom
- 약한 Vignette
- 색보정
- 선택적 Film Grain
- Motion Blur는 기본 비활성화 권장
- 감염 효과는 접근성 설정으로 낮출 수 있음

---

## 9. UI 아트

- 모서리를 둥글린 두꺼운 패널과 버튼. 반경과 크기는 `ui-ux-design.md` §15.3을 따름
- 본문은 한글 가독성 우선. 굵은 산세리프와 검정 외곽선을 기본으로 씀
- 아이콘은 24px에서도 형태가 구분돼야 하고 얇은 선 대신 채운 면을 씀
- 모든 플레이어 색상에 번호 또는 기호 병행
- 미션 UI는 월드 장비의 색을 이어받되 패널 형태는 위 공통 문법을 씀
- 빌런 전용 UI는 자주·적색이지만 외부 캐릭터 연출은 정상 미션과 같음

의료 단말을 연상시키는 직사각형 패널은 1.5까지의 방향이었고, 어두운 월드에서 글자가
읽히지 않아 1.6에서 위 문법으로 교체했다. 월드 장비 자체는 계속 직사각형 산업 디자인을
유지하므로 UI와 월드는 의도적으로 다른 형태 언어를 쓴다.

### 9.1 폰트 에셋

| 항목 | 값 |
| --- | --- |
| 위치 | `game/Assets/_Project/Art/Fonts/` |
| 라이선스 | 재배포 가능한 것만 사용 (OFL 등). 상업 폰트와 OS 번들 폰트는 저장소에 넣지 않음 |
| TMP 에셋 | `Bold`와 `Regular` 두 벌을 생성 |
| 문자 집합 | 한글 상용 2,350자 + ASCII + 사용 기호 |
| 렌더 모드 | SDF (외곽선과 크기 배율을 쓰기 때문) |

폰트를 저장소에 포함하는 이유는 OS 폴백 폰트를 쓰면 PC마다 다른 글자가 나오고
Bold·외곽선이 적용되지 않기 때문이다. 추가한 폰트는
`THIRD_PARTY_NOTICES.md`에 라이선스를 기재한다.

UI 세부 화면과 상태는 `ui-ux-design.md`를 따른다.

---

## 10. 오디오 디자인 원칙

소리는 분위기뿐 아니라 실제 게임 정보다.

1. 괴물 반응을 일으키는 게임 소음과 분위기 소리를 구분한다.
2. 게임 소음은 발생 원인과 위치를 학습할 수 있어야 한다.
3. 괴물의 접근은 화면 밖에서도 판단 가능해야 한다.
4. 음악이 발소리, 경보와 감염 타이머를 가리지 않는다.
5. 중요한 소리는 방향 표시와 자막을 지원한다.

---

## 11. 오디오 믹서 구조

```text
Master
├── Music
├── Ambience
├── SFX
│   ├── Player
│   ├── Monster
│   ├── Mission
│   ├── World
│   └── Danger
└── UI
```

회의 중에는 월드 SFX와 Ambience를 낮추되 완전히 끄지 않고 정지된 연구소의 잔향을 약하게 남긴다. `Danger`와 UI는 별도 볼륨 조절을 제공하되 0으로 만들 경우 시각 경고가 반드시 켜져야 한다.

---

## 12. 필수 사운드 목록

### 플레이어

- 바닥 재질별 발소리 최소 3변형
- 미션 상호작용
- 배터리 들기·놓기
- 물림 반응
- 해독제 사용
- 사망

### 괴물

- 먼 거리 울음
- 순찰 발소리
- 소리 인식 반응
- 가속 추격
- 표적 발견
- 물기
- 표적 상실 수색
- 격리실 등장

### 미션·시설

- 퓨즈 성공·실패
- 차단기 성공·실패
- CCTV 부팅
- 시료 분류 성공·실패
- 스피커 시작·종료
- 격리문 경보·개방
- 제작 시작·완료
- 조명 단계 복구
- 최종 문 개방

퓨즈 실패는 프로토타입 단계에서도 실제 전기 스파크 효과음, 화면 경고와
게임플레이 소음 사건을 동시에 발생시킨다.

### 감염·UI

- 감염 시작
- 60초·30초 경고
- 심장 박동 단계
- 해독제 성공
- 회의 호출
- 투표 시작·종료
- 퇴출
- 승리·패배

### 환경

- 환풍기
- 전기 허밍
- 먼 금속 충격
- 냉장실 냉각기
- 서버실 팬
- 경보 잔향
- RX-9 가스 방출

---

## 13. 공간 음향 규칙

- 괴물, 미션, 스피커, 문은 XY 위치를 사용하는 공간 AudioSource를 사용한다.
- UI와 개인 감염 심장 박동은 2D로 재생한다.
- 벽 하나를 사이에 둔 소리는 저역 통과와 볼륨 감소를 적용할 수 있다.
- 실제 괴물 AI의 소음 반경과 플레이어가 듣는 오디오 감쇠는 별도 데이터지만 모순되지 않게 조정한다.
- 스피커 소리는 방 바깥에서도 방향을 찾을 수 있어야 한다.
- 같은 효과음이 반복될 때 피치와 샘플 변형으로 피로를 줄인다.

---

## 14. 에셋 명명

| 종류 | 접두사 | 예시 |
| --- | --- | --- |
| Sprite | `S_` | `S_Player_Body`, `S_Monkey_Idle_N` |
| Sprite Atlas | `SA_` | `SA_Laboratory`, `SA_Characters` |
| Material | `M_` | `M_LabWall_White` |
| Texture | `T_` | `T_Monkey_BaseColor` |
| Animation | `A_` | `A_Monkey_Bite` |
| Animator Controller | `AC_` | `AC_Player` |
| VFX Prefab | `VFX_` | `VFX_RedVentSmoke` |
| Audio Clip | `SFX_` / `AMB_` / `MUS_` | `SFX_Monkey_Bite_01` |
| Audio Mixer | `MX_` | `MX_Master` |

버전과 `final`을 파일명에 붙이지 않는다. 변경 이력은 Git으로 관리한다.

---

## 15. 외부 에셋 규칙

- 구매·무료 여부와 관계없이 상업적 사용 가능 여부를 확인한다.
- 원본 링크, 제작자, 라이선스, 수정 여부를 기록한다.
- 서로 다른 스타일의 에셋은 머티리얼과 비율을 통일한 뒤 사용한다.
- 외부 패키지 전체를 `Assets/_Project` 안으로 이동하지 않는다.
- 샘플 씬과 미사용 고해상도 파일은 프로젝트에 포함하지 않는다.
- 라이선스 전문 또는 고지 의무는 `THIRD_PARTY_NOTICES.md`에 남긴다.

---

## 16. 에셋 제작 우선순위

1. 플레이어 캡슐과 괴물 캡슐로 그레이박스 검증
2. 모듈형 벽·바닥·문
3. 플레이어와 괴물 기본 모델·애니메이션
4. 핵심 미션 스테이션과 영구 단서
5. 정보성 SFX와 기본 VFX
6. 방별 색·표지·조명
7. 공용 프롭과 환경음
8. 결과·엔딩 연출
9. 장식과 추가 변형

레이아웃이 확정되기 전에 장식용 방 에셋을 대량 제작하지 않는다.
